<%= form_for @category do |f| %>
	<%= render partial: 'users/errors', locals: { model: @category, item: "category" } %>

	<div class="form-group">
		<%= f.label :name, "Name:" %>
		<%= f.text_field :name, class: "form-control" %>
	</div>

	<div class="action">
		<%= f.submit %>
	</div>
<% end %>	